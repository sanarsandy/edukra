# {{PROJECT_NAME}}

Full-stack web application built with Go (Echo) and Nuxt.js.

## Quick Start

1. Copy `.env.example` to `.env` and configure
2. Run `docker compose up -d`
3. Access frontend at http://localhost:3000
4. Access backend API at http://localhost:8080

## Documentation

See `project-template/SPEC.md` for full technical specifications.

